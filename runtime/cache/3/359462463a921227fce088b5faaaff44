{"content":{"image":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"image\" value=\"\"  id=\"cover_id_image\"  />\n    <div id=\"fileList_image\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_image\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_image = $('#fileList_image'),\n        // 优化retina, 在retina下这个值是2\n        ratio_image = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_image = 100 * ratio_image,\n    thumbnailHeight_image = 100 * ratio_image,\n\n    // Web Uploader实例\n    uploader_image;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_image = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_image',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_image.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_image.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_image.html( $li );\n\n        // 创建缩略图\n        uploader_image.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_image, thumbnailHeight_image );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_image.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_image.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_image\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_image.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_image.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","app_cover_img":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"app_cover_img\" value=\"986\"  id=\"cover_id_app_cover_img\"  />\n    <div id=\"fileList_app_cover_img\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_app_cover_img\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_app_cover_img = $('#fileList_app_cover_img'),\n        // 优化retina, 在retina下这个值是2\n        ratio_app_cover_img = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_app_cover_img = 100 * ratio_app_cover_img,\n    thumbnailHeight_app_cover_img = 100 * ratio_app_cover_img,\n\n    // Web Uploader实例\n    uploader_app_cover_img;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_app_cover_img = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_app_cover_img',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"986\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-17/upload_35f25190eb0f7c351d5bf50157316191.png\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_app_cover_img.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_app_cover_img.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_app_cover_img.html( $li );\n\n        // 创建缩略图\n        uploader_app_cover_img.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_app_cover_img, thumbnailHeight_app_cover_img );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_app_cover_img.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_app_cover_img.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_app_cover_img\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_app_cover_img.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_app_cover_img.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","web_cover_img":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n    <input type=\"hidden\" name=\"web_cover_img\" value=\"971\"  id=\"cover_id_web_cover_img\"  />\n    <div id=\"fileList_web_cover_img\" class=\"uploader-list\"></div>\n    <div id=\"filePicker_web_cover_img\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_web_cover_img = $('#fileList_web_cover_img'),\n        // 优化retina, 在retina下这个值是2\n        ratio_web_cover_img = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_web_cover_img = 100 * ratio_web_cover_img,\n    thumbnailHeight_web_cover_img = 100 * ratio_web_cover_img,\n\n    // Web Uploader实例\n    uploader_web_cover_img;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_web_cover_img = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/adminfile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_web_cover_img',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"971\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-09/upload_95086a34426998fdf1db864fb80a57c2.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_web_cover_img.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_web_cover_img.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_web_cover_img.html( $li );\n\n        // 创建缩略图\n        uploader_web_cover_img.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_web_cover_img, thumbnailHeight_web_cover_img );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_web_cover_img.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_web_cover_img.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_web_cover_img\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_web_cover_img.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_web_cover_img.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>"},"expire":1535083143338}