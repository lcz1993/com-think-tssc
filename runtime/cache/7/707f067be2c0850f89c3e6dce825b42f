{"content":{"WEB_INDEX_PAGE_IMG":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n<input type=\"hidden\" name=\"WEB_INDEX_PAGE_IMG\" value=\"1014\"  id=\"cover_id_WEB_INDEX_PAGE_IMG\"  />\n<div id=\"fileList_WEB_INDEX_PAGE_IMG\" class=\"uploader-list\"></div>\n<div id=\"filePicker_WEB_INDEX_PAGE_IMG\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_WEB_INDEX_PAGE_IMG = $('#fileList_WEB_INDEX_PAGE_IMG'),\n        // 优化retina, 在retina下这个值是2\n        ratio_WEB_INDEX_PAGE_IMG = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_WEB_INDEX_PAGE_IMG = 100 * ratio_WEB_INDEX_PAGE_IMG,\n    thumbnailHeight_WEB_INDEX_PAGE_IMG = 100 * ratio_WEB_INDEX_PAGE_IMG,\n\n    // Web Uploader实例\n    uploader_WEB_INDEX_PAGE_IMG;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_WEB_INDEX_PAGE_IMG = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/homefile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_WEB_INDEX_PAGE_IMG',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"1014\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-23/upload_00908a83a06ec6e92d9acb685085b7cf.png\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_INDEX_PAGE_IMG.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_WEB_INDEX_PAGE_IMG.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_INDEX_PAGE_IMG.html( $li );\n\n        // 创建缩略图\n        uploader_WEB_INDEX_PAGE_IMG.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_WEB_INDEX_PAGE_IMG, thumbnailHeight_WEB_INDEX_PAGE_IMG );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_WEB_INDEX_PAGE_IMG.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_WEB_INDEX_PAGE_IMG.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_WEB_INDEX_PAGE_IMG\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_WEB_INDEX_PAGE_IMG.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_WEB_INDEX_PAGE_IMG.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","WEB_ABOUT_LOGO":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n<input type=\"hidden\" name=\"WEB_ABOUT_LOGO\" value=\"1015\"  id=\"cover_id_WEB_ABOUT_LOGO\"  />\n<div id=\"fileList_WEB_ABOUT_LOGO\" class=\"uploader-list\"></div>\n<div id=\"filePicker_WEB_ABOUT_LOGO\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_WEB_ABOUT_LOGO = $('#fileList_WEB_ABOUT_LOGO'),\n        // 优化retina, 在retina下这个值是2\n        ratio_WEB_ABOUT_LOGO = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_WEB_ABOUT_LOGO = 100 * ratio_WEB_ABOUT_LOGO,\n    thumbnailHeight_WEB_ABOUT_LOGO = 100 * ratio_WEB_ABOUT_LOGO,\n\n    // Web Uploader实例\n    uploader_WEB_ABOUT_LOGO;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_WEB_ABOUT_LOGO = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/homefile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_WEB_ABOUT_LOGO',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"1015\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-23/upload_0216cc10265105714f9185d9290be928.png\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_ABOUT_LOGO.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_WEB_ABOUT_LOGO.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_ABOUT_LOGO.html( $li );\n\n        // 创建缩略图\n        uploader_WEB_ABOUT_LOGO.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_WEB_ABOUT_LOGO, thumbnailHeight_WEB_ABOUT_LOGO );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_WEB_ABOUT_LOGO.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_WEB_ABOUT_LOGO.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_WEB_ABOUT_LOGO\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_WEB_ABOUT_LOGO.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_WEB_ABOUT_LOGO.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","WEB_INDEX_BOTTOM_IMG":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n<input type=\"hidden\" name=\"WEB_INDEX_BOTTOM_IMG\" value=\"\"  id=\"cover_id_WEB_INDEX_BOTTOM_IMG\"  />\n<div id=\"fileList_WEB_INDEX_BOTTOM_IMG\" class=\"uploader-list\"></div>\n<div id=\"filePicker_WEB_INDEX_BOTTOM_IMG\">选择图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_WEB_INDEX_BOTTOM_IMG = $('#fileList_WEB_INDEX_BOTTOM_IMG'),\n        // 优化retina, 在retina下这个值是2\n        ratio_WEB_INDEX_BOTTOM_IMG = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_WEB_INDEX_BOTTOM_IMG = 100 * ratio_WEB_INDEX_BOTTOM_IMG,\n    thumbnailHeight_WEB_INDEX_BOTTOM_IMG = 100 * ratio_WEB_INDEX_BOTTOM_IMG,\n\n    // Web Uploader实例\n    uploader_WEB_INDEX_BOTTOM_IMG;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_WEB_INDEX_BOTTOM_IMG = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/homefile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_WEB_INDEX_BOTTOM_IMG',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"\";\n    if(cover_id != 0){\n        var picurl = \"/static/noimg.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_INDEX_BOTTOM_IMG.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_WEB_INDEX_BOTTOM_IMG.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_INDEX_BOTTOM_IMG.html( $li );\n\n        // 创建缩略图\n        uploader_WEB_INDEX_BOTTOM_IMG.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_WEB_INDEX_BOTTOM_IMG, thumbnailHeight_WEB_INDEX_BOTTOM_IMG );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_WEB_INDEX_BOTTOM_IMG.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_WEB_INDEX_BOTTOM_IMG.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_WEB_INDEX_BOTTOM_IMG\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_WEB_INDEX_BOTTOM_IMG.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_WEB_INDEX_BOTTOM_IMG.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","WEB_LOGO_ICO":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n<input type=\"hidden\" name=\"WEB_LOGO_ICO\" value=\"987\"  id=\"cover_id_WEB_LOGO_ICO\"  />\n<div id=\"fileList_WEB_LOGO_ICO\" class=\"uploader-list\"></div>\n<div id=\"filePicker_WEB_LOGO_ICO\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_WEB_LOGO_ICO = $('#fileList_WEB_LOGO_ICO'),\n        // 优化retina, 在retina下这个值是2\n        ratio_WEB_LOGO_ICO = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_WEB_LOGO_ICO = 100 * ratio_WEB_LOGO_ICO,\n    thumbnailHeight_WEB_LOGO_ICO = 100 * ratio_WEB_LOGO_ICO,\n\n    // Web Uploader实例\n    uploader_WEB_LOGO_ICO;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_WEB_LOGO_ICO = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/homefile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_WEB_LOGO_ICO',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"987\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-21/upload_e752f012a05ef4ed81f97d72631fc395.png\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_LOGO_ICO.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_WEB_LOGO_ICO.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_LOGO_ICO.html( $li );\n\n        // 创建缩略图\n        uploader_WEB_LOGO_ICO.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_WEB_LOGO_ICO, thumbnailHeight_WEB_LOGO_ICO );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_WEB_LOGO_ICO.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_WEB_LOGO_ICO.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_WEB_LOGO_ICO\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_WEB_LOGO_ICO.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_WEB_LOGO_ICO.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","WEB_LOGO":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n<input type=\"hidden\" name=\"WEB_LOGO\" value=\"988\"  id=\"cover_id_WEB_LOGO\"  />\n<div id=\"fileList_WEB_LOGO\" class=\"uploader-list\"></div>\n<div id=\"filePicker_WEB_LOGO\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_WEB_LOGO = $('#fileList_WEB_LOGO'),\n        // 优化retina, 在retina下这个值是2\n        ratio_WEB_LOGO = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_WEB_LOGO = 100 * ratio_WEB_LOGO,\n    thumbnailHeight_WEB_LOGO = 100 * ratio_WEB_LOGO,\n\n    // Web Uploader实例\n    uploader_WEB_LOGO;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_WEB_LOGO = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/homefile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_WEB_LOGO',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"988\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-21/upload_df528ff3788a70d0ab523717f2e9b6c2.png\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_LOGO.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_WEB_LOGO.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_LOGO.html( $li );\n\n        // 创建缩略图\n        uploader_WEB_LOGO.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_WEB_LOGO, thumbnailHeight_WEB_LOGO );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_WEB_LOGO.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_WEB_LOGO.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_WEB_LOGO\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_WEB_LOGO.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_WEB_LOGO.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>","WEB_RECORD":"<link rel=\"stylesheet\" href=\"/static/admin/js/webuploader/webuploader.css\" type=\"text/css\">\n\n<input type=\"hidden\" name=\"WEB_RECORD\" value=\"994\"  id=\"cover_id_WEB_RECORD\"  />\n<div id=\"fileList_WEB_RECORD\" class=\"uploader-list\"></div>\n<div id=\"filePicker_WEB_RECORD\">修改图片</div>\n\n<script src=\"/static/admin/js/webuploader/webuploader.js\" type=\"text/javascript\"></script>\n<script type=\"text/javascript\">\n    $list_WEB_RECORD = $('#fileList_WEB_RECORD'),\n        // 优化retina, 在retina下这个值是2\n        ratio_WEB_RECORD = window.devicePixelRatio || 1,\n\n        // 缩略图大小\n        thumbnailWidth_WEB_RECORD = 100 * ratio_WEB_RECORD,\n    thumbnailHeight_WEB_RECORD = 100 * ratio_WEB_RECORD,\n\n    // Web Uploader实例\n    uploader_WEB_RECORD;\n\n    // 初始化Web Uploader\n    // 初始化Web Uploader\n    var uploader_WEB_RECORD = WebUploader.create({\n        // 选完文件后，是否自动上传。\n        auto: true,\n        // swf文件路径\n        swf: '/static/admin/js/webuploader/Uploader.swf',\n        // 文件接收服务端。\n        server: '/ext/attachment/homefile/uploadpic',\n        // 选择文件的按钮。可选。\n        // 内部根据当前运行是创建，可能是input元素，也可能是flash.\n        pick: {\n            id:'#filePicker_WEB_RECORD',\n            multiple: false\n        },\n        // 只允许选择图片文件。\n        accept: {\n            title: 'Images',\n            extensions: 'gif,jpg,jpeg,bmp,png',\n            mimeTypes: 'image/jpg,image/jpeg,image/png,image/gif,image/bmp'\n        }\n    });\n    //当有图片时加载图片\n    var cover_id=\"994\";\n    if(cover_id != 0){\n        var picurl = \"/upload/picture/2018-08-22/upload_1a55436722d5010a01457e6b6614194c.jpg\"\n        var $li = $(\n            '<div id=\"' + cover_id + '\" class=\"file-item thumbnail thumb-lg\">' +\n            '<img src=\"'+picurl+'\">' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_RECORD.html( $li );\n    }\n    // 当有文件添加进来的时候\n    uploader_WEB_RECORD.on( 'fileQueued', function( file ) {\n        var $li = $(\n            '<div id=\"' + file.id + '\" class=\"file-item thumbnail\">' +\n            '<img>' +\n            '<div class=\"info\">' + file.name + '</div>' +\n            '</div>'\n            ),\n            $img = $li.find('img');\n\n        $list_WEB_RECORD.html( $li );\n\n        // 创建缩略图\n        uploader_WEB_RECORD.makeThumb( file, function( error, src ) {\n            if ( error ) {\n                $img.replaceWith('<span>不能预览</span>');\n                return;\n            }\n\n            $img.attr( 'src', src );\n        }, thumbnailWidth_WEB_RECORD, thumbnailHeight_WEB_RECORD );\n    });\n\n    // 文件上传过程中创建进度条实时显示。\n    uploader_WEB_RECORD.on( 'uploadProgress', function( file, percentage ) {\n\n        var $li = $( '#'+file.id ),\n            $percent = $li.find('.progress span');\n\n        // 避免重复创建\n        if ( !$percent.length ) {\n            $percent = $('<p class=\"progress\"><span></span></p>')\n                .appendTo( $li )\n                .find('span');\n        }\n\n        $percent.css( 'width', percentage * 100 + '%' );\n    });\n\n    // 文件上传成功，给item添加成功class, 用样式标记上传成功。\n    uploader_WEB_RECORD.on( 'uploadSuccess', function( file,res ) {\n        $( '#'+file.id ).addClass('upload-state-done');\n        $(\"#cover_id_WEB_RECORD\").val(res);\n    });\n\n    // 文件上传失败，现实上传出错。\n    uploader_WEB_RECORD.on( 'uploadError', function( file ) {\n        var $li = $( '#'+file.id ),\n            $error = $li.find('div.error');\n\n        // 避免重复创建\n        if ( !$error.length ) {\n            $error = $('<div class=\"error\"></div>').appendTo( $li );\n        }\n\n        $error.text('上传失败');\n    });\n\n    // 完成上传完了，成功或者失败，先删除进度条。\n    uploader_WEB_RECORD.on( 'uploadComplete', function( file ) {\n        $( '#'+file.id ).find('.progress').remove();\n    });\n</script>"},"expire":1535081002992}